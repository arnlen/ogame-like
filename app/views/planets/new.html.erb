<h1>First planet</h1>
<p>Give a name to your first planet</p>
<%= form_for @planet do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.submit "Validate" %>
<% end %>